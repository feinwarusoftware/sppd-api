{
  "routes": [
    {
      "method": "GET",
      "path": "/cards",
      "handler": "card.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cards/count",
      "handler": "card.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cards/list",
      "handler": "card.list",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cards/image/:image",
      "handler": "card.image",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cards/:id",
      "handler": "card.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/cards",
      "handler": "card.create",
      "config": {
        "policies": []
      }
    }
  ]
}
